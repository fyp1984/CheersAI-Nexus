[Unit]
Description=CheersAI Nexus Feedback Service
Documentation=https://github.com/cheersai/nexus-backend
After=network.target nexus-user-management.service
Wants=network-online.target

[Service]
Type=simple
User=nexus
Group=nexus
WorkingDirectory=/home/nexus/app/backend

# JAR 文件
Environment="JAVA_HOME=/usr/lib/jvm/java-21-openjdk"
Environment="SPRING_PROFILES_ACTIVE=prod"
Environment="SERVER_PORT=8084"
Environment="APP_HOME=/home/nexus/app"

# JVM 参数
ExecStart=/usr/bin/java \
    -Xms512m \
    -Xmx1024m \
    -XX:+UseG1GC \
    -XX:MaxGCPauseMillis=200 \
    -Djava.security.egd=file:/dev/./urandom \
    -Dspring.config.location=/home/nexus/app/config/feedback/ \
    -jar /home/nexus/app/backend/nexus-feedback.jar

# 停止配置
ExecStop=/bin/kill -SIGTERM $MAINPID
SuccessExitStatus=143

# 重启策略
Restart=on-failure
RestartSec=10
StartLimitBurst=3
StartLimitIntervalSec=60

# 日志配置
StandardOutput=journal
StandardError=journal
SyslogIdentifier=nexus-feedback

# 权限配置
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/home/nexus/app/logs /home/nexus/app/config

[Install]
WantedBy=multi-user.target
